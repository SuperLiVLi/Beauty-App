<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqut.beautifulapp.dao.CommentDao">

    <resultMap type="com.cqut.beautifulapp.entity.Comment" id="CommentMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="towhom" column="towhom" jdbcType="VARCHAR"/>
        <result property="author" column="author" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryCommentById" resultMap="CommentMap">
        select
               content,
               author
        from comment
        where towhom = #{id}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into comment(content, towhom, author)
        values (#{content}, #{towhom}, #{author})
    </insert>


</mapper>

